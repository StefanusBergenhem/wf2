{
  "name": "readview-ts",
  "version": "0.1.0",
  "private": true,
  "description": "Reference TypeScript/JS read-view extractor — derives the common IR (read-view-extractor-guide.md §4) from source via the TypeScript Compiler API.",
  "type": "commonjs",
  "bin": {
    "readview-ts": "dist/extract.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "extract": "node dist/extract.js"
  },
  "dependencies": {
    "typescript": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
