# Transient generated output (read-views, IRs, models, clusters, briefs, scratch)
.tmp/

# Shared Python venv for wf's own machinery
tools/.venv/
**/__pycache__/

# Vendored graph library for offline rendering (fetched once during setup)
tools/discover/vendor/

# Language-extractor build artifacts (regenerated from source)
tools/discover/readview-go/readview
tools/discover/readview-ts/node_modules/
tools/discover/readview-ts/dist/

# validation-repo: generated install output — not tracked (regenerate via the
# refresh command in validation-repo/README.md). Only the fixture source is kept.
validation-repo/.claude/
validation-repo/.pi/
validation-repo/.opencode/
validation-repo/.wf/
