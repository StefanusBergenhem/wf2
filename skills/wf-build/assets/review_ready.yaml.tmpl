# Build → review handoff marker. Transient, worktree-local. Its PRESENCE is the
# ready_for_review verdict — that is its whole job. The reviewer judges the committed code
# and tests from the diff, the contract, and the [REQ] tags, never from a build self-report,
# so this file carries nothing but the task id it belongs to.
task_id: "<id>"
