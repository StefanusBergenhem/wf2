# Build halted: it needs to touch a file outside files_to_touch. Transient,
# worktree-local. Its PRESENCE is the build_blocked verdict; the orchestrator classifies
# the gap, widens the contract's scope, and re-dispatches the build.
task_id: "<id>"
required_files:
  - "<path the contract must add to files_to_touch>"
reason: "<one line: why the contract cannot be satisfied without these files>"
