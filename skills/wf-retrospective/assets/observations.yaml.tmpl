# .wf/observations.yaml — the admission buffer in front of paths.learnings (committed).
# Written only by wf-retrospective. NO design role reads this file.
#
# What has been seen ONCE. A second sighting promotes an entry into a learnings file,
# where it starts costing every later design dispatch context; until then it waits here.
# Entries carry no id — one minted for something never seen twice moves the counter for
# nothing. `wf observations age` bounds the file at hygiene.observations_max, archiving
# what it drops.
#
# Entry shape: see the wf-retrospective skill.
version: 1
observations: []
