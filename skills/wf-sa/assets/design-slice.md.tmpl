<!--
  DESIGN-SLICE — wf-sa → wf-tl handover for ONE buildable increment. TRANSIENT.
  wf-sa cuts it from the committed design backlog; retained through the build and drained
  at sprint close. Gitignored, never durable, never read across cuts — the backlog
  (committed) is what persists. Reference the brief and drill-cache by path — do NOT
  restate structure here.
-->

# Design-slice — <change summary in one line>

**Serves:** CAP-NNN, L-NNN          <!-- the capabilities and/or learnings this change drives -->
**System view:** see `<paths.discover_brief>`; depth in `<paths.drill_cache>/<digests consulted>`
**Design diagram:** `<paths.design_view>` (transient — regenerate from `<paths.design_graph>`)
**Date:** YYYY-MM-DD

## Design narrative

<!-- The story of the change, in plain prose — REQUIRED; `wf slice check` fails the
     handover without it. Copied (or excerpted for this increment) from the backlog
     design's Narrative. It describes only THIS CHANGE — the existing system stays in
     the brief and drill digests, referenced by path, never restated here:
     - the shape chosen and the force that drove it;
     - how each end-to-end behaviour flows through the components IN ORDER, wiring
       included (composition root, orchestration) — the intended path, so a build
       cannot ship the behaviour wired some other way;
     - each touched component's role in the story — a sentence or two per component,
       never a section. -->

<the shape, the flow end-to-end (wiring included), each component's role in the change>

## Component requirements

<!-- The increment's requirements, copied from the backlog: EARS-light, each owned by ONE
     component, tracing to a capability or learning. Ids are the backlog's REPO-UNIQUE ids
     (REQ-<n>). Copy a `proof: inspection — <fact>` annotation through unchanged — a
     dropped annotation loses the requirement's only proof declaration.
     No acceptance criteria here — the Tech Lead writes those. -->

- **REQ-12** — <EARS statement>  *(owner: <component> · CAP-NNN)*
- **REQ-13** — <EARS statement>  *(owner: <component> · L-NNN)*
- **REQ-14** — <EARS statement>  *(owner: <component> · CAP-NNN)*

## System test cases

<!-- The end-to-end behaviour(s) this slice delivers — YOU write these; wf-tl plans each
     into the sprint as its own e2e task. Each is a Gherkin-light, black-box scenario that
     proves the assembled path (no component-seam mocks) and COVERS A CAPABILITY (never a
     component requirement). Ids are repo-unique SYS-TC-<n> from their own lane. Omit the
     section for a single-component change with no observable end-to-end behaviour. -->

- **SYS-TC-1:** <clear title of the end-to-end behaviour>
  **Covers:** CAP-NNN            <!-- the capability this behaviour proves; never REQ-<n> -->
  - **Given** <initial state / preconditions>
  - **When** <the external trigger or action>
  - **Then** <the observable, verifiable end state or output>

## Supersedes

<!-- Shipped behaviour this slice invalidates — each entry ratified by the human at
     alignment before it may appear here. A superseded end-to-end scenario is named by
     its SYS-TC id (complete-sprint sweeps its tag at close — it must not survive the
     sprint); superseded component behaviour has no durable id, so name its proving
     test file(s). wf-tl folds the named tests into the successor's task (or a
     dedicated removal task when there is no successor). Omit the section when nothing
     shipped is invalidated. -->

- **SYS-TC-2** — <one-line reason it no longer holds> · retired, no successor
- <the component behaviour retired> — proven by `<test file path>` · successor **REQ-12**

## Architecture moves

<!-- Only if this change reshapes structure: split / merge / new component / changed
     dependency. Each move is a decision (an ADR below) AND a reason to re-run
     discover after the change lands. Omit the section if structure is unchanged. -->

- <move> — recorded in ADR-NNN; re-run discover after merge.

## Interface contracts

<!-- For each NEW component and each widened shared seam this slice introduces: the
     concrete shape — signature / struct / endpoint request+response — agreed here,
     because no source exists yet for wf-tl or the build to read it from. Behaviour
     lives in the requirements; only the shape lives here. Omit the section when the
     slice touches existing interfaces only. -->

- **<component or seam>** — serves REQ-NNN; bound by ADR-NNN

      <the signature / struct / endpoint request+response shape>

## NFR & authz

<!-- The slice-level pass over performance envelopes and authorization. Every entry is
     either a requirement id or an explicit recorded deferral — never a silent absence.
     Envelope: any trigger whose work scales with data volume carries a measurable bound
     (requirement-syntax.md, five elements). Authz: any new or changed entry point
     carries an authorization requirement. -->

- **Envelope:** REQ-NNN bounds <the fan-out / latency>  — or **deferred:** <what · why · revisit when>
- **Authz:** REQ-NNN guards <the new/changed entry points>  — or **deferred:** <what · why · revisit when>

## Decisions binding this change

- **New:** ADR-NNN — <title>
- **Standing:** ADR-NNN — <title> (already governs <component>)

## Assumptions requiring confirmation

<!-- Every interpretive leap made turning a driver into this design — a reading the
     capability's/learning's wording does not force. Each is presented to the human at
     alignment and recorded here with the reading chosen AND the reading rejected.
     Mark it CONFIRMED only after the human ratifies it — `wf slice check` fails the
     handover on any UNCONFIRMED entry. Omit the section when the drivers admitted
     only one reading. -->

- **A-1 · CONFIRMED** — CAP-NNN read as <the reading designed to>, not <the reading rejected>.

## Soundness

<!-- From the Phase 5 soundness walk: one line per design heuristic
     (references/design-heuristics.md) — pass with its justification, or the conflict found
     and how it was resolved. -->

- **Boundary & responsibility (SRP):** <pass + why, or conflict + resolution>
- **Ownership:** <…>
- **Dependency direction:** <…>
- **Interface stability:** <…>
- **Extension over modification:** <…>
- **Allocation completeness:** <…>
- **Move discipline:** <…>

## Risks for the Tech Lead

- <anything to watch when decomposing — a fragile seam, an ordering constraint,
  an external dependency. Omit if none.>
