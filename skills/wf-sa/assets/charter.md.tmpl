<!--
  CHARTER — the durable direction (committed). Lives at `paths.charter`.
  Owner: the wf-sa session, with human approval. The design role reads it, never writes it.
  Bound by `hygiene.charter_max` lines: over the cap means design has leaked in — cut back
  to direction. Delete every element the repo has already reached.
  Placeholders below are the shape, not content — replace each with this project's own.
-->

# Charter

## Target shape

<!-- The fat-marker sketch of where the system is going: the few major parts and how they
     relate. Not components, not interfaces. -->

<the shape, in a few lines>

## Ranked forces

<!-- What this system optimizes for, in priority order, each with the trade-off its
     ranking accepts. -->

1. **<force>** — accepts <what is given up for it>

## Domain language

<!-- The terms the system must use for its concepts, and the terms it must not. -->

- **<term>** — <what it means here> · not <the term it replaces>

## Sequencing rationale

<!-- What must come before what, and why. -->

- <this> before <that> — <the reason the order is forced>

## No-go zones

<!-- What the design role must not design, and until when. -->

- <what is off the table> — until <the condition that lifts it>
