<!--
  DESIGN BACKLOG — wf-sa's designed-but-unbuilt work. COMMITTED, and DRAINING.
  wf-sa appends a design when it shapes a solution for a capability or learning;
  `wf pipeline complete-sprint` trims each id at sprint close once its covering task
  merged (the merge record), and removes a design when its last id goes. When the
  backlog is empty, every designed thing has shipped. This is NOT a durable spec — it
  drains to empty. Load-bearing decisions live in the ADRs (durable); the structure of
  BUILT work is discover's (re-derived). Reference the brief and drill-cache by path —
  do NOT restate EXISTING structure here (the Narrative describes only the change).
-->

# Design backlog

<!-- One block per shaped change. Two draining id lanes, each REPO-UNIQUE
     (id_counters.req / id_counters.sys_tc in .wf/config.yaml are the high-water marks):
     - component requirements (REQ-<n>) — EARS, owned by ONE component, with its driver (CAP/L)
       so a later slice can map it to its capability;
     - system test cases (SYS-TC-<n>) — capability-level end-to-end proofs, each covering a CAP.
     complete-sprint removes each id whose covering task merged; keep the bullet shapes
     below (`- **REQ-<n>**`, `- **SYS-TC-<n>**`, indented continuations) so the
     mechanical trim can parse them. -->

## <design title> — serves CAP-NNN / L-NNN

**Decisions:** ADR-NNN (new) · ADR-NNN (standing, already governs <component>)
**Moves:** <split / merge / new component / changed edge> — recorded in ADR-NNN; re-run discover after build.

**Narrative:** <the design's prose story — the shape and why, each end-to-end flow
through the components in order (wiring included), each touched component's role in
the change. Copied into every slice cut from this design. Describes only the change;
never restates existing structure.>

**Component requirements:**
- **REQ-12** — <EARS statement>  *(owner: <component> · CAP-NNN)*
- **REQ-13** — <EARS statement>  *(owner: <component> · L-NNN)*
- **REQ-14** — <EARS statement>  *(owner: <component> · L-NNN · proof: inspection — <the
  source fact that proves it>)*  <!-- gate/config-proven only; no test can observe it -->

**System test cases:**
- **SYS-TC-1** — <end-to-end behaviour title>  *(covers CAP-NNN)*

**Supersedes:**  <!-- shipped behaviour this design invalidates; omit when none. A
     SYS-TC id's tag is swept at sprint close; component behaviour is named by its
     proving test file(s), which the slice hands wf-tl to fold into a task. -->
- **SYS-TC-2** — <one-line reason it no longer holds> · retired, no successor
- <the component behaviour retired> — proven by `<test file path>` · successor **REQ-12**

<!-- repeat one ## block per shaped change; delete this scaffold's example block -->
