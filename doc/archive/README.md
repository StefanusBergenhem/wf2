# doc/archive — spent work documents

Change records, design interviews, and run analyses whose work has shipped. Kept
for the record only.

**Do not read anything here as current.** Every document describes the repo at the
moment it was written, and several describe machinery that has since been deleted
(`wf-orchestrate`, `wf-swa`, `wf-spec-fix`, the EARS/REQ tag layer, the design
backlog, the slice, increments, sub-layers). For what wf2 is now, read `CLAUDE.md`
and `doc/RUNBOOK.md`; for what the code does, read the code.

The live pair in `doc/` is `CANDIDATES.md` (the parking lot) and `RUNBOOK.md` (the
operator's manual). Anything still open when a document was archived was moved into
`CANDIDATES.md` first.
